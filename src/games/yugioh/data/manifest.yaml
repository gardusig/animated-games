# Required catalog manifest — seed/CI checks row counts
version: 1
source: ygoprodeck-demo
cards:
  file: cards.csv
  min_rows: 900
  id_column: id
  index_file: card_list.csv
images:
  strategy: url_only
  fill_script: CLI commands / src/generate_cards_csv.py --fill-missing-images
